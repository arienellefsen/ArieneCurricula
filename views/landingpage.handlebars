
<div class="container">
<<<<<<< HEAD
    <!-- Login Area -->
    <div class="row">
        <div class="col-xs-6 col-sm-6 col-lg-6">
            <div class="page-header header_site">
                <h1 class="logo">Placeholder for logo</h1>
            </div>
        </div>
        <div class="col-xs-6 col-sm-6 col-lg-6">
        {{# if login}}
                <a href="/create" class="btn btn-danger">
                    <span class="fa"></span> New
                </a> 
                <a href="/userview" class="btn btn-danger">
                    <span class="fa"></span> Profile
                </a> 
                <a href="/user/logout" class="btn btn-danger">
                    <span class="logout"></span> Logout
                </a>

        {{else}}
                <a href="/user/signin" class="btn btn-danger">
                    <span class="fa"></span> Log In
                </a> 
                <a href="/user/signup" class="btn btn-danger">
                    <span class="fa"></span> Sign Up
                </a> 

        {{/if}}
        </div>
    </div>
    
=======
>>>>>>> 9602ce6d44edd0b5930d54849efcfe87bc86c9b6
    <div class="row" id="landing-content">
        {{#each curriculaInstance}}
            {{#if this.submited_status}}
            <div class="col-xs-12 col-sm-3 col-lg-3">
                <div class="thumbnail" id="cur{{this.id}}">
                    <a href="curricula/{{this.id}}">
                        <img src="./assets/img/default_curriculum.png" alt="{{this.curricula_name}}"> 
                    </a>
                    <div class="caption"> 
                        <p> {{this.curricula_name}} </p>  
                        <p> {{this.description}} </p>
                        <p> Votes: {{this.votes}} </p>
                        <p> Author: {{this.authorId}} </p>
                    </div>
                </div>
            </div>
            {{/if}}
        {{/each}}
    </div>
</div>


